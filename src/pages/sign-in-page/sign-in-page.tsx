import { CheckEmail, ForgotPassword, Header, SignIn, SignUp } from '@/components'

export const SignInPage = () => {
  return (
    <div>
      <Header />
      <SignIn onSubmit={() => {}} />
    </div>
  )
}
